// FieldFlow configuration
// -----------------------------------------------------------------
// Paste your Supabase anon (publishable) key between the quotes on
// the SUPABASE_ANON_KEY line below, then commit the change.
//
// Supabase dashboard: Project Settings -> API Keys -> anon / public
//
// Leave the key empty and FieldFlow keeps working exactly as it does
// today, saving data only in this browser. Once the key is filled in,
// the app switches on accounts and cloud sync.
//
// Never put the service_role / secret key in this file. It is public.
// -----------------------------------------------------------------
window.FIELDFLOW_CONFIG = {
  SUPABASE_URL: 'https://vbszdqxsrmediacvxaie.supabase.co',
  SUPABASE_ANON_KEY: ''
};
